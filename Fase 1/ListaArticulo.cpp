#include <iostream>
#include "ListaArticulo.h"

using namespace std;



/*void ListaArticulo::agregarArti(string dato, int id, int precio,string nombre, string src){
    Categoria* tempocate = cabecera;
    while(tempocate != NULL){
        if(tempocate->dato == dato){;
            Articulo* nuev = new Articulo(id, precio, nombre, src);
            Articulo* inicioar = tempocate->abajo;
            tempocate->abajo = nuev;
            nuev->abajo = iniciocancion;
            break;
        }
        tempocate = tempocate->siguiente;
    }
    if (tempocate == NULL){
        cout<<"No se encontro ninguna categoria"<<endl;
    }
}*/